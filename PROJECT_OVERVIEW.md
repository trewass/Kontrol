# 📋 Kontrol — Обзор проекта

> **MVP агрегатор задач для мебельного производства**
> Telegram + WhatsApp → AI обработка → Единый чат задач с кнопками управления

---

## 🎯 Что решает проект

### Проблема
Задачи мебельного цеха **теряются в 3+ чатах**:
- Telegram группы (Менеджеры, Монтажники, Технологи)
- WhatsApp переписки с клиентами (через amoCRM/Wazzup)
- Голосовые сообщения на бегу

**Результат:** забытые дедлайны, конфликты, недовольные клиенты

### Решение
**Kontrol** собирает все задачи в **один главный чат** с кнопками управления:
```
Рабочие чаты (текст + голос)
    ↓
AI извлекает задачи (GPT-4o-mini + Whisper)
    ↓
Главный чат "ЗАДАЧИ" с кнопками:
[✅ Взял] [🧾 Уточнить] [📅 Перенёс] [☑️ Закрыл] [🧨 Не задача]
```

---

## ✨ Ключевые возможности

### 1. 🎤 Умная обработка сообщений
- **Текст** — обычные сообщения в Telegram/WhatsApp
- **Голос** — голосовые сообщения (автоматическая транскрибация Whisper)
- **Аудио** — аудио файлы
- **Видео** — видео кружки (круглые видео)

### 2. 🧠 AI-извлечение задач
Production промпт с **словарём мебели**:
- Типон, доводчик, сенсор, петли, ручка
- ДСП, МДФ, ЛДСП, кромка, торцовка
- Фасады, столешница, стекляшка
- Распил, кромление, присадка, монтаж, замер

**Результат:** задача в структурированном виде с клиентом, адресом, тегами, дедлайном

### 3. 🎮 Управление статусами
5 кнопок в каждой задаче:
- ✅ **Взял** → IN_PROGRESS (фиксирует исполнителя)
- 🧾 **Уточнить** → CLARIFICATION (нужна информация)
- 📅 **Перенёс** → POSTPONED (отложено)
- ☑️ **Закрыл** → DONE (выполнено)
- 🧨 **Не задача** → REJECTED (удаляет из чата)

### 4. ⏰ Умные напоминания
- **Не взяли:** через N минут после создания
- **Дедлайн 24ч:** за сутки до срока
- **Дедлайн 2ч:** критическое напоминание

### 5. 📊 Веб-админ
Простой интерфейс для владельца:
- Список всех задач с фильтрами
- Поиск по клиенту/объекту
- История изменений каждой задачи

---

## 🏗️ Архитектура

### Стек технологий

| Компонент | Технология | Зачем |
|-----------|-----------|-------|
| **Backend** | Node.js 20 + NestJS + TypeScript | Production-ready framework с DI |
| **Database** | PostgreSQL 16 + Prisma ORM | Надёжная БД + type-safe запросы |
| **Queue** | BullMQ + Redis | Асинхронная обработка (не блокирует webhook) |
| **Bot** | Grammy (Telegram Bot API) | Современная библиотека для TS |
| **AI** | OpenAI API | GPT-4o-mini (задачи) + Whisper-1 (транскрибация) |
| **Views** | Express Handlebars | Server-side рендеринг для админки |
| **Deploy** | Docker Compose | Быстрый запуск (app + postgres + redis) |

### Основные модули

```
src/
├── llm/                # AI: извлечение задач + транскрибация аудио
├── telegram/           # Telegram бот: текст, голос, видео, кнопки
├── wazzup/             # WhatsApp webhooks (через Wazzup)
├── tasks/              # CRUD задач, дедупликация, поиск
├── queue/              # BullMQ worker (обработка сообщений)
├── scheduler/          # Cron напоминаний (каждые 5 мин)
├── admin/              # Веб-интерфейс (список задач + история)
└── database/           # Prisma Client (ORM)
```

### База данных

**4 таблицы:**
1. **tasks** — задачи (title, status, priority, client, deadline, assignee)
2. **task_events** — история изменений (кто/когда/что изменил)
3. **sources** — источники (Telegram группы, Wazzup каналы)
4. **users** — исполнители (маппинг Telegram username → внутренний ID)

**6 статусов задач:**
- NEW → IN_PROGRESS → DONE ✅
- NEW → CLARIFICATION → IN_PROGRESS → DONE
- NEW → POSTPONED → IN_PROGRESS → DONE
- NEW → REJECTED 🧨 (не задача)

---

## 🚀 Быстрый старт

### 1. Установка

```bash
cd /Users/xxx/Projects/Kontrol

# Создать .env
cp .env.example .env
# Заполнить: TELEGRAM_BOT_TOKEN, TELEGRAM_TASKS_CHAT_ID, LLM_API_KEY
```

### 2. Запуск

```bash
# С Docker (рекомендуется)
docker-compose up -d

# Или локально
npm install
npx prisma migrate dev
npm run start:dev
```

### 3. Настройка Telegram бота

1. **Создать бота:** [@BotFather](https://t.me/BotFather) → `/newbot`
2. **Отключить Privacy Mode:** `/setprivacy` → Disable (КРИТИЧНО!)
3. **Создать группу "ЗАДАЧИ"** → добавить бота как админа
4. **Получить chat_id:** отправить сообщение → `/getUpdates`
5. **Настроить webhook:** ngrok + `/setWebhook`

### 4. Проверка

```bash
# Веб-админ
open http://localhost:3000/admin

# Логи
docker-compose logs -f app

# Тестовый запрос
curl -X POST http://localhost:3000/wazzup/webhook \
  -H "Content-Type: application/json" \
  -d @examples/wazzup-webhook-incoming.json
```

---

## 📖 Примеры использования

### Пример 1: Текстовое сообщение

**Вход:**
```
Менеджер в Telegram: "Петя, созвонись с Ивановым до пятницы, он хочет поменять цвет фасадов на венге"
```

**Выход:**
```
Главный чат "ЗАДАЧИ":

🆕 Новая

Созвониться с Ивановым по изменению цвета фасадов

Клиент: Иванов
⏰ Срок: до пятницы
🏷 созвониться, фасады, цвет

[✅ Взял] [🧾 Уточнить]
[📅 Перенёс] [☑️ Закрыл]
[🧨 Не задача]
```

### Пример 2: Голосовое сообщение

**Вход:**
```
Монтажник диктует:
🎤 "Надо срочно поменять типон на двери в офисе на Ленина пять, сенсор глючит"
```

**Обработка:**
```
1. Telegram API → скачивание .ogg файла
2. Whisper API → "Надо срочно поменять типон на двери в офисе на Ленина пять, сенсор глючит"
3. GPT-4o-mini → извлечение задачи
```

**Выход:**
```
Главный чат "ЗАДАЧИ":

🆕 Новая

Поменять типон на двери в офисе на Ленина 5

Сенсор глючит, требуется замена типона
📍 Объект: ул. Ленина 5
🏷 типон, сенсор, монтаж
🔥 СРОЧНО

[Кнопки...]
```

### Пример 3: Взятие задачи

**Действие:**
```
Петя нажимает "✅ Взял"
```

**Результат:**
```
🔵 В работе

Созвониться с Ивановым по изменению цвета фасадов

👷 Взял: @petya_montazhnik
...
```

---

## 📁 Структура файлов

```
Kontrol/
├── README.md                    # Главная документация
├── STRUCTURE.md                 # Архитектура + потоки данных
├── CHANGELOG.md                 # История изменений
├── PROJECT_OVERVIEW.md          # Этот файл (обзор)
│
├── .env.example                 # Пример конфигурации
├── docker-compose.yml           # Оркестрация (app + postgres + redis)
├── Dockerfile                   # Multi-stage build
├── package.json                 # Зависимости
├── tsconfig.json                # TypeScript config
│
├── prisma/
│   └── schema.prisma            # Схема БД (4 таблицы)
│
├── src/
│   ├── main.ts                  # Точка входа
│   ├── app.module.ts            # Корневой модуль
│   │
│   ├── llm/                     # AI (GPT + Whisper)
│   │   ├── llm.service.ts       # extractTask() + transcribeAudio()
│   │   └── llm.module.ts
│   │
│   ├── telegram/                # Telegram бот
│   │   ├── telegram.service.ts  # Обработка сообщений + кнопок
│   │   ├── telegram.controller.ts # Webhook endpoint
│   │   └── telegram.module.ts
│   │
│   ├── wazzup/                  # WhatsApp integration
│   │   ├── wazzup.service.ts
│   │   ├── wazzup.controller.ts
│   │   └── wazzup.module.ts
│   │
│   ├── tasks/                   # Логика задач
│   │   ├── tasks.service.ts     # CRUD + дедупликация
│   │   └── tasks.module.ts
│   │
│   ├── queue/                   # BullMQ
│   │   ├── message.processor.ts # Worker обработки
│   │   └── queue.module.ts
│   │
│   ├── scheduler/               # Cron напоминаний
│   │   ├── scheduler.service.ts
│   │   └── scheduler.module.ts
│   │
│   ├── admin/                   # Веб-админ
│   │   ├── admin.controller.ts
│   │   └── admin.module.ts
│   │
│   ├── database/                # Prisma
│   │   ├── prisma.service.ts
│   │   └── database.module.ts
│   │
│   └── common/
│       └── schemas/
│           └── task-extraction.schema.ts # JSON Schema + AJV
│
├── views/                       # Handlebars шаблоны
│   ├── index.hbs                # Список задач
│   └── task-detail.hbs          # Детали задачи
│
└── examples/                    # Примеры для тестирования
    ├── telegram-message.json
    ├── telegram-callback.json
    ├── wazzup-webhook-incoming.json
    ├── wazzup-webhook-outgoing.json
    ├── wazzup-webhook-complex.json
    └── test-scenarios.md        # 9 сценариев тестирования
```

---

## 🧪 Тестирование

### 9 готовых сценариев

См. [examples/test-scenarios.md](examples/test-scenarios.md):

1. ✅ Создание задачи из Telegram
2. ✅ Взятие задачи в работу (кнопка)
3. ✅ Отклонение флуда
4. ✅ Напоминания (не взяли)
5. ✅ **Голосовое сообщение (транскрибация)**
6. ✅ Wazzup webhook (WhatsApp)
7. ✅ Дедупликация
8. ✅ Веб-админ
9. ✅ Срочная задача

### Ручное тестирование

```bash
# 1. Отправить текстовое сообщение в группу
# 2. Отправить голосовое сообщение
# 3. Проверить главный чат → появилась задача
# 4. Нажать "✅ Взял" → статус обновился
# 5. Подождать 30 мин → пришло напоминание
```

---

## 💰 Стоимость использования

### OpenAI API

| Модель | Цена | Применение | Средняя стоимость |
|--------|------|-----------|-------------------|
| **GPT-4o-mini** | $0.15 / 1M input tokens | Извлечение задач | $0.0003 на задачу |
| **Whisper-1** | $0.006 / минута | Транскрибация аудио | $0.002 на 20 сек голоса |

**Пример для малого цеха:**
- 100 текстовых задач/день = $0.03
- 50 голосовых задач/день = $0.10
- **Итого:** ~$4/месяц ($0.13/день)

### Инфраструктура

- **Локально:** бесплатно (свой сервер)
- **VPS (1 GB RAM):** $5-10/месяц (Hetzner, DigitalOcean)
- **Ngrok Pro:** $8/месяц (для webhook)

**Итого MVP:** ~$17-22/месяц

---

## 🔐 Безопасность

### Реализовано
- ✅ Дедупликация (защита от спама)
- ✅ Валидация JSON Schema (проверка ответов LLM)
- ✅ Игнорирование главного чата (не создаём задачи из задач)
- ✅ Privacy Mode проверка (чеклист настройки)

### TODO для продакшена
- [ ] Rate limiting на webhooks
- [ ] IP whitelist для /admin
- [ ] Шифрование чувствительных данных в БД
- [ ] Ротация API ключей
- [ ] Логирование в Sentry

---

## 📊 Метрики успеха

### KPI проекта
1. **% не потерянных задач:** 95%+ (было ~60%)
2. **Время реакции:** < 30 мин (было ~4 часа)
3. **Дедлайны:** 90%+ в срок (было ~70%)
4. **Удовлетворённость команды:** 8+/10

### Мониторинг
- Кол-во задач/день
- Среднее время взятия задачи
- % напоминаний
- % отклонённых (not_task)

---

## 🛣️ Roadmap

### v1.1 ✅ (текущая версия)
- ✅ Транскрибация голосовых сообщений (Whisper API)

### v1.2 (планируется)
- [ ] Распознавание текста в скриншотах (Vision API)
- [ ] Голосовые ответы (TTS для уведомлений)
- [ ] Поддержка файлов (PDF, Excel с заказами)

### v1.3
- [ ] Интеграция с реальным amoCRM API (не только webhook)
- [ ] Автоматическое распределение задач (ML модель)
- [ ] Мобильное приложение для монтажников

### v2.0
- [ ] Multi-tenancy (несколько цехов в одной БД)
- [ ] Analytics dashboard
- [ ] Интеграция с 1С

---

## 🤝 Поддержка

### Документация
- [README.md](README.md) — инструкции запуска
- [STRUCTURE.md](STRUCTURE.md) — архитектура
- [CHANGELOG.md](CHANGELOG.md) — история версий
- [examples/test-scenarios.md](examples/test-scenarios.md) — тестирование

### FAQ
См. раздел FAQ в [README.md](README.md)

### Баги/Вопросы
- GitHub Issues (если проект на GitHub)
- Или прямой контакт с разработчиком

---

## 📝 Лицензия

UNLICENSED — частный проект для внутреннего использования

---

## 🎉 Заключение

**Kontrol** — это не просто бот, а **система управления задачами**, которая:
- ⚡ **Экономит время** — не нужно искать задачи в 5 чатах
- 🎯 **Снижает стресс** — все задачи в одном месте с кнопками
- 📈 **Увеличивает производительность** — напоминания + дедлайны
- 🧠 **Использует AI** — автоматическое извлечение + транскрибация

**MVP готов к использованию прямо сейчас!**

```bash
docker-compose up -d
open http://localhost:3000/admin
```

---

**Сделано с ❤️ для мебельного производства**
